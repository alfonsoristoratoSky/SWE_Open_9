<h2>You are deleting {{restaurant.0.name}}</h2>
<dl>
    <dt>Name</dt>
    <dd>{{restaurant.0.name}}</dd>
    <dt>Image</dt>
    <dd>
        <img class="round-image" src='{{restaurant.0.imagelink}}' alt='{{restaurant.0.name}}'/>
    </dd>
</dl>
<form method="post" action="/restaurants/{{restaurant.0.id}}/delete?_method=DELETE">
<div> Click this to delete your Restaurant

            <input type="submit" value="Submit">


        </div>
</form>
{{> backToRestaurants}}