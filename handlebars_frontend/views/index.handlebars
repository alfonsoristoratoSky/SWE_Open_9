
<div>
Welcome! There are {{restaurants.length}} restaurants in the DB

<a href='/restaurants'>See the restaurants</a>
</div>
<div class="col-md-12">
  {{#if restaurants.length }}
    {{#each restaurants }}
      {{> restaurantItem this }}
    {{/each}}
  {{else}}
    <p>No restaurants left :(</p>
  {{/if}}
</div>